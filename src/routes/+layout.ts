// This is a static app. No server-side rendering. Generate files at build time
// Default is undefined. Is true whenever ssr is false to avoid unnecessary server work.
export const prerender = true;
